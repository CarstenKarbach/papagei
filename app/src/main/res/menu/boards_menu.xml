<menu xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/menu_item_edit"
        android:title="@string/edit"
        android:icon="@drawable/ic_edit_solid"
        android:iconTint="@color/iconMainColor"
        app:showAsAction="always"
        />
    <item android:id="@+id/menu_item_export"
        android:title="@string/exportieren"
        android:icon="@drawable/ic_share_solid"
        android:iconTint="@color/iconMainColor"
        app:showAsAction="always"
        />
    <item android:id="@+id/menu_item_delete"
        android:title="@string/delete"
        android:icon="@drawable/ic_trash_solid"
        android:iconTint="@color/iconMainColor"
        app:showAsAction="always"
        />
</menu>